if (grid ['1'] + grid ['2'] grid ['3'] == 'O') {
    document.querySelector("p").replaceChild(document.createTextNode(`WINNER!!!!`), document.querySelector('p').firstChild);
  }
if (grid ['4'] + grid ['5'] + grid ['6'] == 'O') {
    document.querySelector("p").replaceChild(document.createTextNode(`WINNER!!!!`), document.querySelector('p').firstChild);
  }
  if (grid ['7'] + grid ['8'] + grid ['9'] == 'O') {
    document.querySelector("p").replaceChild(document.createTextNode(`WINNER!!!!`), document.querySelector('p').firstChild);
  }
  if (grid ['1'] + grid ['4'] + grid ['7'] == 'O') {
    document.querySelector("p").replaceChild(document.createTextNode(`WINNER!!!!`), document.querySelector('p').firstChild);
  }
  if (grid ['2'] + grid ['5'] + grid ['8'] == 'O') {
    document.querySelector("p").replaceChild(document.createTextNode(`WINNER!!!!`), document.querySelector('p').firstChild);
  }
  if (grid ['3'] + grid ['6'] + grid ['9'] == 'O') {
    document.querySelector("p").replaceChild(document.createTextNode(`WINNER!!!!`), document.querySelector('p').firstChild);
  }
  if (grid ['1'] + grid ['5'] + grid ['9'] == 'O') {
    document.querySelector("p").replaceChild(document.createTextNode(`WINNER!!!!`), document.querySelector('p').firstChild);
  }
  if (grid ['3'] + grid ['5'] + grid ['7'] == 'O') {
    document.querySelector("p").replaceChild(document.createTextNode(`WINNER!!!!`), document.querySelector('p').firstChild);
  }
  if (grid ['1'] + grid ['2'] + grid ['3'] == 'X') {
    document.querySelector("p").replaceChild(document.createTextNode(`WINNER!!!!`), document.querySelector('p').firstChild);
  }
  if (grid ['4'] + grid ['5'] + grid ['6'] == 'X') {
    document.querySelector("p").replaceChild(document.createTextNode(`WINNER!!!!`), document.querySelector('p').firstChild);
  }
  if (grid ['7'] + grid ['8'] + grid ['9'] == 'X') {
    document.querySelector("p").replaceChild(document.createTextNode(`WINNER!!!!`), document.querySelector('p').firstChild);
  }
  if (grid ['1'] + grid ['4'] + grid ['7'] == 'X') {
    document.querySelector("p").replaceChild(document.createTextNode(`WINNER!!!!`), document.querySelector('p').firstChild);
  }
  if (grid ['2'] + grid ['5'] + grid ['8'] == 'X') {
    document.querySelector("p").replaceChild(document.createTextNode(`WINNER!!!!`), document.querySelector('p').firstChild);
  }
  if (grid ['3'] + grid ['6'] + grid ['9'] == 'X') {
    document.querySelector("p").replaceChild(document.createTextNode(`WINNER!!!!`), document.querySelector('p').firstChild);
  }
  if (grid ['1'] + grid ['5'] + grid ['9'] == 'X') {
    document.querySelector("p").replaceChild(document.createTextNode(`WINNER!!!!`), document.querySelector('p').firstChild);
  }
  if (grid ['3'] + grid ['5'] + grid ['7'] == 'X') {
    document.querySelector("p").replaceChild(document.createTextNode(`WINNER!!!!`), document.querySelector('p').firstChild);
  }
